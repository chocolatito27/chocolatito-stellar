{
  "name": "chocolatito-stellar",
  "version": "0.1.0",
  "description": "Un agente de IA que firma, por cada tarea, el pago exacto de lo que esa tarea costo: en USDC sobre Stellar testnet.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "demo": "node --experimental-strip-types guiones/demo.ts",
    "probar": "node --experimental-strip-types guiones/probar-verificacion.ts",
    "tipos": "tsc -p ."
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
